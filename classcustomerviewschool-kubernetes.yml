spring:
  data:
    mongodb:
      host: localhost
      database: School
  cloud:
    stream:
      kafka:
        binder:
          brokers: localhost
          zkNodes: localhost

school:
  classcustomerview:
    transaction:
      timeStoredInMinutes: 60

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
