server:
  port: 8084

spring:
  cloud:
    stream:
      bindings:
          ClassCustomerEvent:
            group: classcustomerview

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true

